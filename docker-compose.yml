# ─────────────────────────────────────────────────────────────────────────────
# Roognis AI — Docker Compose (Local Development + Staging)
# All services are ENV-var driven for zero-code cloud migration
# ─────────────────────────────────────────────────────────────────────────────

volumes:
  postgres_data:
  chroma_data:
  ollama_models:
  comfy_models:
  file_storage:

services:

  # ── API Gateway ─────────────────────────────────────────────────────────────
  traefik:
    image: traefik:v2.11
    ports:
      - "80:80"
      - "8080:8080"  # Dashboard
    volumes:
      - ./traefik/traefik.yml:/etc/traefik/traefik.yml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    restart: unless-stopped

  # ── Databases ───────────────────────────────────────────────────────────────
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: roognis
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d roognis"]
      interval: 5s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  chromadb:
    image: chromadb/chroma:latest
    volumes:
      - chroma_data:/chroma/chroma
    restart: unless-stopped

  # ── AI Layer ─────────────────────────────────────────────────────────────────
  ollama:
    image: ollama/ollama:latest
    volumes:
      - ollama_models:/root/.ollama
      - ./scripts/ollama-init.sh:/ollama-init.sh:ro
    entrypoint: ["/bin/sh", "/ollama-init.sh"]
    healthcheck:
      # Passes only after qwen2.5 is fully pulled and ready
      test: ["CMD-SHELL", "ollama list 2>/dev/null | grep -q qwen2.5"]
      interval: 30s
      timeout: 10s
      retries: 20
      start_period: 120s
    restart: unless-stopped

  comfyui:
    image: ghcr.io/ai-dock/comfyui:latest
    ports:
      - "8188:8188"
    volumes:
      - comfy_models:/opt/ComfyUI/models
      - file_storage:/opt/ComfyUI/output
    restart: unless-stopped

  # ── Application Services ─────────────────────────────────────────────────────

  auth:
    build: ./services/auth
    ports:
      - "3001:3001"
    command: >
      sh -c "NODE_TLS_REJECT_UNAUTHORIZED=0 npx prisma db push --schema=prisma/schema.prisma &&
             node scripts/seed.js &&
             node server.js"
    environment:
      DATABASE_URL: "postgresql://postgres:${DB_PASSWORD}@postgres:5432/roognis?schema=auth_db"
      JWT_SECRET: ${JWT_SECRET}
      DEMO_SCHOOL_ID: ${DEMO_SCHOOL_ID}
      PORT: 3001
      NODE_ENV: production
      NODE_TLS_REJECT_UNAUTHORIZED: "0"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "curl -sf http://localhost:3001/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.auth.rule=PathPrefix(`/api/auth`)"
      - "traefik.http.routers.auth.entrypoints=web"
      - "traefik.http.services.auth.loadbalancer.server.port=3001"
      - "traefik.http.routers.auth.middlewares=cors@file"
    restart: unless-stopped

  ai:
    build: ./services/ai
    command: >
      sh -c "[ -f prisma/schema.prisma ] &&
             npx prisma db push --schema=prisma/schema.prisma ||
             true && node server.js"
    environment:
      DATABASE_URL: "postgresql://postgres:${DB_PASSWORD}@postgres:5432/roognis?schema=ai_db"
      JWT_SECRET: ${JWT_SECRET}
      OLLAMA_URL: "http://ollama:11434"
      RAG_SERVICE_URL: "http://rag:3003"
      ANALYTICS_URL: "http://analytics:3004"
      INTERNAL_SERVICE_TOKEN: ${INTERNAL_SERVICE_TOKEN}
      COMFYUI_URL: "http://comfyui:8188"
      FILE_STORAGE_PATH: /app/storage
      PORT: 3002
      NODE_ENV: production
      # Cloud swap — set to use Anthropic instead of Ollama
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      # Cloud swap — set to use S3 instead of Docker volume
      AWS_S3_BUCKET: ${AWS_S3_BUCKET:-}
      AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:-}
      AWS_SECRET_ACCESS_KEY: ${AWS_SECRET_ACCESS_KEY:-}
      AWS_REGION: ${AWS_REGION:-ap-south-1}
    volumes:
      - file_storage:/app/storage
      - ./seed-data:/app/seed-data:ro
    depends_on:
      postgres:
        condition: service_healthy
      ollama:
        condition: service_healthy
      rag:
        condition: service_started
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.ai.rule=PathPrefix(`/api/ai`)"
      - "traefik.http.routers.ai.entrypoints=web"
      - "traefik.http.services.ai.loadbalancer.server.port=3002"
      - "traefik.http.routers.ai.middlewares=cors@file"
    restart: unless-stopped

  rag:
    build: ./services/rag
    environment:
      DATABASE_URL: "postgresql://postgres:${DB_PASSWORD}@postgres:5432/roognis"
      CHROMA_URL: "http://chromadb:8000"
      OLLAMA_URL: "http://ollama:11434"
      DEMO_SCHOOL_ID: ${DEMO_SCHOOL_ID}
      JWT_SECRET: ${JWT_SECRET}
      FILE_STORAGE_PATH: /app/storage
      PORT: 3003
      # Cloud swap — set to use Pinecone instead of ChromaDB
      PINECONE_API_KEY: ${PINECONE_API_KEY:-}
      PINECONE_ENV: ${PINECONE_ENV:-}
    volumes:
      - file_storage:/app/storage
      - ./seed-data:/app/seed-data:ro
    depends_on:
      postgres:
        condition: service_healthy
      chromadb:
        condition: service_started
      ollama:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.rag.rule=PathPrefix(`/api/rag`)"
      - "traefik.http.routers.rag.entrypoints=web"
      - "traefik.http.services.rag.loadbalancer.server.port=3003"
      - "traefik.http.routers.rag.middlewares=cors@file"
    restart: unless-stopped

  analytics:
    build: ./services/analytics
    command: >
      sh -c "[ -f prisma/schema.prisma ] &&
             npx prisma db push --schema=prisma/schema.prisma ||
             true && node server.js"
    environment:
      DATABASE_URL: "postgresql://postgres:${DB_PASSWORD}@postgres:5432/roognis?schema=analytics_db"
      JWT_SECRET: ${JWT_SECRET}
      INTERNAL_SERVICE_TOKEN: ${INTERNAL_SERVICE_TOKEN}
      PORT: 3004
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.analytics.rule=PathPrefix(`/api/analytics`)"
      - "traefik.http.routers.analytics.entrypoints=web"
      - "traefik.http.services.analytics.loadbalancer.server.port=3004"
      - "traefik.http.routers.analytics.middlewares=cors@file"
    restart: unless-stopped

  frontend:
    build: ./frontend
    environment:
      NEXT_PUBLIC_API_URL: "http://localhost/api"
      PORT: 3000
      NODE_ENV: production
    depends_on:
      auth:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.rule=PathPrefix(`/`)"
      - "traefik.http.routers.frontend.entrypoints=web"
      - "traefik.http.services.frontend.loadbalancer.server.port=3000"
      - "traefik.http.routers.frontend.middlewares=cors@file"
    restart: unless-stopped
